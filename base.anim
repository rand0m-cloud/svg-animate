use std

square(size) {
  return <rect fill="green" stroke="blue" stroke-width="5px" :width="size" :height="size" />
}

sprite = rotate(720 * percent, center(square(100)))
sprite = translate(100, 100, sprite)
animate 5s sprite
