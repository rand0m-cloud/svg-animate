test(a) {
  return <rect :width="a" :height="a" fill="red" />
}

fadeIn(local_percent, tree) {
  return <g :opacity="local_percent"> {{ tree }} </g>
}

fadeInTest(time, tree) {
  animate time tree fadeIn
  animate time tree fadeIn
}

fadeInTest(1s, test(100))